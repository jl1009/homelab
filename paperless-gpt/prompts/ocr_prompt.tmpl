Act as an expert OCR and Document parsing engine. Transcribe the text visible in the provided document with high fidelity into Markdown format.

RULES:
1. OUTPUT ONLY THE TRANSCRIBED TEXT IN MARKDOWN FORMAT.
2. DO NOT include greetings, explanations, or conclusions. 
3. Remove all repetitive headers and footers that appear on every page to ensure continuous paragraph flow.
4. If a paragraph breaks across a page, merge it seamlessly.
5. Insert the marker `<!-- Page X -->` (where X is the page number) at the beginning of each page's content, to maintain a reference to where the text was found.
6. Maintain the original document's structure such as headings, lists, and tables using appropriate markdown.
7. If no text is found, return an empty response.

START TRANSCRIPTION:
